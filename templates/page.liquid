<section style="padding:40px 0 80px;">
  <div class="container" style="max-width:800px;margin:0 auto;padding:0 24px;">
    <h1 style="font-size:36px;font-weight:900;letter-spacing:-0.04em;text-transform:uppercase;margin-bottom:24px;">{{ page.title }}</h1>
    <div style="font-size:16px;color:var(--brand-muted);line-height:1.8;">
      {{ page.content }}
    </div>
  </div>
</section>
